<h1>Members Only</h1>

<ul>
  <% @posts.each do |post| %>
    <li> <%= post.title %> </li>
    <li> <%= if user_signed_in?
              post.user.email
            else
              "Anonymous"
            end 
          %> 
    </li>
  <% end %>
</ul>
